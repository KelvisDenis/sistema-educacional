@model IEnumerable<SistemaEducacional.Models.DocenteModel>
@{
    ViewData["Title"] = "Docente";
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - SistemaEducacional</title>
    <link rel="stylesheet" href="~/css/Indexcss.css" />
</head>
<nav class="navbar bg-body-tertiary">
    <div style="margin-left:350px" class="container-fluid">
        <form asp-controller="Docente" asp-action="Buscar" class="d-flex" role="search">
            <input class="form-control me-2" type="search" placeholder="Buscar" aria-label="Search" name="nome">
            <button class="btn btn-outline-success" type="submit">Pesquisar</button>
        </form>
    </div>
</nav>
<table class="table">
    <thead>
        <tr>
            <th scope="col">Nome</th>
            <th scope="col">CPF</th>
            <th scope="col">Data Nascimento</th>
            <th scope="col">Opções</th>
        </tr>
    </thead>
    <tbody>
        @foreach(var item in Model)
        {
            <tr>
                <th scope="row">@item.Nome</th>
                <td>@item.Cpf</td>
                <td>@item.DataNascimento.Value</td>
                <td>
                    <a asp-route-id="@item.Id" asp-controller="Docente" asp-action="Alterar">Alterar</a>
                    <a asp-route-id="@item.Id" asp-controller="Docente" asp-action="Excluir">Excluir</a>
                </td>
            </tr>
        }
       
    </tbody>
</table>

